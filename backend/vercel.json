{
  "version": 2,
  "builds": [
    {
      "src": "api/[...all].ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api",
      "dest": "api/[...all].ts"
    },
    {
      "src": "/api/(.*)",
      "dest": "api/[...all].ts"
    }
  ]
}

